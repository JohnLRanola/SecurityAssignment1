#include <stdio.h>
#include <string.h>

int main() {
    char input[100];
    char keyword[5];

    printf("Enter TEXT in UPPERCASE: ");
    scanf("%s", input);
    
    printf("Enter a 4 letter KEYWORD in UPPERCASE: ");
    scanf("%s", keyword);
}